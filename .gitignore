# Secrets — never commit
.env
.env.local
.env.*.local
secrets/
secrets/*.txt

# Docker compose override for local dev
docker-compose.override.yml

# Migration artifacts
migration/

# OS / editor cruft
.DS_Store
*.swp
*.bak
.idea/
.vscode/
