"""3D Reconstruction — WebODM user plugin.

Turns the outputs of a completed task (DSM / DTM / orthophoto / LAS-LAZ point
cloud / existing textured model) into a compact, georeferenced, web-ready GLB
that the built-in 3D viewer displays. See ``workflow.run`` for the entry point
and ``docs/plugins/3d-reconstruction.md`` for the user guide.
"""

__version__ = "1.0.0"
